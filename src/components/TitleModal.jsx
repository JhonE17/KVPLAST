import React from 'react';

export const TitleModal = ({title}) => {
  return (
    <h4 className='text-2xl leading-relaxed text-center text-gray-500 dark:text-gray-400'>
      {title}
    </h4>
  );
};
